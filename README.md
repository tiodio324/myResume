# 📄 myResume - Конструктор Резюме и Портфолио

<div align="center">
  <h3>🎯 СВОБОДА ОТ ШАБЛОНОВ | ПОЛНЫЙ КОНТРОЛЬ НАД КОНТЕНТОМ 🎯</h3>
  <p><i>Создан из-за разочарования в HeadHunter и других платформах</i></p>
</div>

## 💎 Почему этот проект?

Я пользовался HeadHunter годами, и меня всегда бесило:
- **Шаблонность** - все резюме выглядят одинаково, никакой индивидуальности
- **Ограничения** - нельзя добавить столько навыков, должностей и т.д., сколько хочешь, формат строго регламентирован
- **Невозможность показать реальные работы** - где моя галерея проектов? Где ссылки на демо?
- **Невозможность оставить контакт рекомендующего человека**
- **Отсутствие гибкости** - захотел добавить слоган или грейд? Не тут-то было

Поэтому я сделал **myResume** - конструктор, который даёт полную свободу. Хочешь 50 проектов с галереями? Пожалуйста. Нужно показать коммерческие и pet-проекты раздельно? Легко. Захотел добавить рекомендации с фотками коллег? Вперёд!

## ⚡ Что умеет этот конструктор

### 📝 Редактирование на лету
- **Инлайн-редактирование**: кликнул на поле - редактируешь. Никаких отдельных форм и модалок
- **Живое обновление**: все изменения сразу сохраняются в Firebase на лету
- **Умные расчёты**: общий стаж считается автоматически, возраст тоже - просто укажи даты

### 🎨 Гибкая структура портфолио
- **Три категории проектов**: Pet-проекты, личные разработки и коммерческие работы
- **Избранные проекты**: отмечай лучшие работы - они попадут в карусель на главной
- **Галереи изображений**: каждый проект может иметь сколько угодно скриншотов
- **Статусы проектов**: "В разработке", "Завершён", "Заморожен" и т.д. с цветовой индикацией
- **И многое другое**

### 👤 Расширенная модель резюме
- **Базовая инфа**: имя, профессия, био, аватарка (все редактируется инлайн)
- **Опыт работы**: компания, должность, период, зарплата, технологии, описание
- **Образование и курсы**: учебные заведения, сертификаты, специализации
- **Контакты**: email, телефон, Telegram, GitHub, LinkedIn
- **Желаемая ЗП**: текущая и желаемая зарплата, тип занятости, готовность к командировкам
- **И многое другое**

### 🔧 Удобные фичи
- **Дублирование резюме**: нужно создать резюме для другой специализации? Дублируй существующее одним кликом
- **Админ-режим**: простая аутентификация для редактирования (через пароль в .env)
- **Адаптивность**: работает на всех устройствах
- **Без бэкенда**: все данные в Firebase Realtime Database

> В отличие от HeadHunter, здесь ты сам решаешь, что и как показывать. Никаких ограничений, никаких шаблонов.

## 🛠️ Технологии

**Frontend Stack:**
- React 19 + TypeScript 5.9
- Vite 7 (rolldown-vite) с SWC для молниеносной сборки
- MobX 6 для state management (проще и быстрее Redux)
- React Router DOM 7 для навигации
- Swiper 12 для каруселей и галерей проектов

**Backend & Database:**
- Firebase Realtime Database (без собственного сервера!)
- Base64 хранение изображений (для простоты демо)

**Стили:**
- SCSS с CSS Grid и Flexbox
- Адаптивный дизайн под все экраны

## 📋 Что понадобится

- Node.js 18+ и npm
- Firebase проект (можно использовать существующий из репозитория или создать свой)
- Пароль для админ-панели (задаётся в `.env`)

## 🔧 Быстрая установка (5 минут)

1. Клонируй репозиторий
   ```bash
   git clone <repo-url>
   cd myResume
   ```

2. Установи зависимости
   ```bash
   npm install
   ```

3. Настрой окружение
   ```bash
   # Создай .env файл в корне проекта
   echo VITE_ADMIN_PASSWORD=your-secret-password > .env
   ```

4. (Опционально) Настрой свой Firebase
   
   По умолчанию проект использует мой Firebase - он открыт для демо. Если хочешь использовать свою базу:
   
   - Создай проект в [Firebase Console](https://console.firebase.google.com/)
   - Включи **Realtime Database**
   - Скопируй конфиг и замени его в `src/firebase.ts`:
   
   ```ts
   const firebaseConfig = {
     apiKey: "твой-api-key",
     authDomain: "твой-project.firebaseapp.com",
     databaseURL: "https://твой-project.firebaseio.com",
     projectId: "твой-project-id",
     // ...
   };
   ```
   
   - Для разработки можно временно открыть правила БД (не делай так в проде!):
   
   ```json
   {
     "rules": {
       ".read": true,
       ".write": true
     }
   }
   ```

5. Запускай!
   ```bash
   npm run dev
   ```
   
   Откроется на `http://localhost:5173`. Выбери резюме (или создай новое после входа в админку).

## 🚀 Запуск и деплой

### Для разработки
```bash
npm run dev      # Запуск dev-сервера с HMR
```

### Сборка для продакшена
```bash
npm run build    # TypeScript + Vite сборка в dist/
npm run preview  # Локальный предпросмотр prod-сборки
```

### Деплой
Проект полностью статический - просто загрузи содержимое `dist/` куда угодно:
- **Vercel** - самый простой вариант, подключи GitHub репо и готово
- **Netlify** - аналогично Vercel, поддержка из коробки
- **GitHub Pages** - бесплатно, но нужно настроить base path в `vite.config.ts`
- **Firebase Hosting** - логично, раз уже используешь Firebase DB
- Любой CDN или хостинг статики

**Важно**: не забудь передать `VITE_ADMIN_PASSWORD` через переменные окружения при сборке!

## ⚙️ Структура проекта

```
myResume/
├── src/
│   ├── components/          # Переиспользуемые компоненты
│   │   ├── EditableField.tsx    # Инлайн-редактор (основа всего UX)
│   │   ├── AddButton.tsx        # Кнопка добавления элементов
│   │   ├── FileUpload.tsx       # Загрузка изображений в base64
│   │   ├── Header.tsx           # Навигация с категориями проектов
│   │   ├── Modal.tsx            # Модальные окна
│   │   └── ...
│   ├── pages/               # Страницы приложения
│   │   ├── ResumeSelection.tsx  # Выбор/создание резюме + админ-вход
│   │   ├── ResumeEdit.tsx       # Редактор резюме (полная форма)
│   │   ├── Main.tsx             # Главная портфолио (обо мне + проекты)
│   │   ├── ProjectsPage.tsx     # Список проектов по категории
│   │   └── ProjectDetail.tsx    # Детальная страница проекта с галереей
│   ├── store/               # MobX состояние
│   │   ├── AuthStore.ts         # Авторизация (localStorage токен)
│   │   └── ResumeStore.ts       # Резюме и проекты (Firebase CRUD)
│   ├── helpers/             # Утилиты
│   │   └── index.ts             # Форматирование дат, расчёт стажа/возраста
│   ├── styles/              # SCSS стили (глобальные + по компонентам)
│   ├── types/               # TypeScript типы
│   │   └── index.ts             # Resume, Project, Experience и др.
│   ├── firebase.ts          # Инициализация Firebase + сервис
│   ├── Router.tsx           # React Router настройка
│   └── App.tsx              # Корневой компонент
├── dist/                    # Продакшен сборка (после npm run build)
└── ...
```

## 🗺️ Маршруты приложения

| Путь | Описание | Доступ |
|------|----------|--------|
| `/` | Выбор резюме из списка | Все |
| `/resume/edit/new` | Создание нового резюме | Только админ |
| `/resume/edit/:resumeId` | Редактирование резюме | Только админ |
| `/main` | Главная страница портфолио | После выбора резюме |
| `/projects/pet` | Пет-проекты | После выбора резюме |
| `/projects/personal` | Личные проекты | После выбора резюме |
| `/projects/commercial` | Коммерческие проекты | После выбора резюме |
| `/project/:projectId` | Детали проекта | После выбора резюме |

**Защита маршрутов:**
- Маршруты `/main`, `/projects/*`, `/project/*` редиректят на `/` если резюме не выбрано
- Редактирование доступно только после входа в админку (проверка в `AuthStore`)

## 🗃️ Модель данных (Firebase структура)

Данные хранятся в Firebase Realtime Database по следующей структуре:

```
/
├── resume-list/
│   └── {resumeId}/                    # Краткая инфа для списка выбора
│       ├── name: "Иван Иванов"
│       ├── profession: "Frontend Developer"
│       └── avatar: "data:image/png;base64,..."
│
└── resumes/
    └── {resumeId}/                    # Полные данные резюме
        ├── name: "Иван Иванов"
        ├── profession: "Frontend Developer"
        ├── bio: "Опытный разработчик..."
        ├── skills: ["React", "TypeScript", ...]
        ├── experience: [{...}, {...}]
        ├── education: [{...}]
        ├── contactInfo: {...}
        ├── petProjects/               # Проекты хранятся отдельно
        │   ├── {projectId}/
        │   │   ├── title: "Awesome App"
        │   │   ├── description: "..."
        │   │   ├── technologies: ["React", ...]
        │   │   ├── images: ["data:image/...", ...]
        │   │   └── ...
        │   └── ...
        ├── personalProjects/
        │   └── ...
        └── commercialProjects/
            └── ...
```

### Основные типы (TypeScript)

<details>
<summary>Показать интерфейсы</summary>

```typescript
// Резюме
interface Resume {
  id: string;
  name: string;
  profession: string;
  slogan?: string;
  specialization?: string;
  grade?: 'Junior' | 'Middle' | 'Senior' | null;
  avatar: string;                      // base64
  bio: string;
  skills: string[];
  experience: Experience[];
  education: Education[];
  courses?: Course[];
  desiredSalary?: number | null;
  currentSalary?: number | null;
  employmentType?: string[];
  businessTrips?: string | null;
  birthDate?: string;                  // ISO
  petProjects: Project[];
  personalProjects: Project[];
  commercialProjects: Project[];
  recommendations: Recommendation[];
  contactInfo: ContactInfo;
}

// Проект
interface Project {
  id: string;
  title: string;
  description: string;                 // Краткое описание
  longDescription: string;             // Подробное описание
  technologies: string[];
  status: ProjectStatus;               // "В разработке", "Завершён", и т.д.
  startDate: string;                   // ISO
  endDate?: string;                    // ISO
  githubUrl?: string;
  websiteUrl?: string;
  images: string[];                    // base64
  featured: boolean;                   // Показывать в "Избранных"
  category: 'pet' | 'personal' | 'commercial';
}

// Опыт работы
interface Experience {
  id: string;
  company: string;
  position: string;
  startDate: string;
  endDate?: string | null;
  isCurrentJob?: boolean;
  description: string;
  technologies: string[];
  salary?: number | null;
}
```

</details>

**Почему проекты хранятся отдельно?**

Изначально я хранил все проекты внутри объекта резюме, но когда проектов стало больше 20-30 штук с кучей изображений, загрузка резюме начала тормозить. Поэтому массивы проектов вынесены в отдельные ноды - так можно загружать их лениво или по категориям.

## 🔐 Админ-доступ и безопасность

### Вход в админку

1. На странице выбора резюме (`/`) нажми иконку входа (правый нижний угол)
2. Логин: `admin`
3. Пароль: значение `VITE_ADMIN_PASSWORD` из `.env`
4. После входа токен сохраняется в `localStorage` - повторный вход не требуется

### Что можно делать в админке

- ✏️ Редактировать любые поля резюме инлайн (прямо на странице `/main`)
- 📝 Полное редактирование через форму (`/resume/edit/:id`)
- ➕ Создавать новые резюме
- 📋 Дублировать существующие резюме
- 🗑️ Удалять резюме
- 🖼️ Загружать изображения для проектов
- ⭐ Отмечать проекты как избранные

### ⚠️ Важно о безопасности

Текущая реализация **НЕ ПОДХОДИТ ДЛЯ ПРОДАКШЕНА** в публичном доступе:

- Пароль передаётся в открытом виде (нет хеширования)
- Firebase правила открыты для чтения/записи
- Нет rate limiting и защиты от CSRF

**Для production-использования:**

1. Включи Firebase Authentication
2. Настрой правила безопасности Firebase:
   ```json
   {
     "rules": {
       ".read": true,  // Чтение всем (это портфолио, пусть смотрят)
       ".write": "auth != null && auth.uid == 'твой-uid'"  // Писать только тебе
     }
   }
   ```
3. Используй Firebase Auth для входа вместо localStorage токена

Но для личного портфолио с 1-2 пользователями текущий подход вполне рабочий - у меня крутится уже несколько месяцев без проблем 😅

## 🎨 Использование и UX

### Инлайн-редактирование

Главная фишка проекта - редактировать можно прямо на странице, без отдельных форм:

1. Залогинься как админ
2. Перейди на страницу портфолио (`/main`)
3. Наведи на любое текстовое поле - оно подсветится
4. Кликни - появится инпут
5. Отредактируй и кликни вне поля или нажми Enter
6. Изменения **автоматически сохраняются** в Firebase

Это работает для:
- Текстовых полей (имя, профессия, био)
- Списков (навыки, технологии)
- Многострочных текстов (описания проектов)

### Добавление элементов

Кнопки с плюсиком (`AddButton`) появляются рядом со списками:

- **Навыки**: добавь новый навык прямо на главной
- **Технологии проекта**: на странице проекта можешь дополнить стек
- **Опыт работы**: в редакторе резюме добавляй новые места работы

### Загрузка изображений

Все изображения конвертируются в base64 и хранятся прямо в БД:

1. Нажми на кнопку загрузки (или drag & drop)
2. Выбери файл (jpg, png, webp)
3. Файл автоматически конвертируется и сохраняется
4. Отображается превью

**Примечание**: для production лучше использовать Firebase Storage вместо base64, но для демо и личного использования это работает отлично (у меня ~15 проектов по 3-4 скриншота = около 2MB данных).

## 🧪 Полезные команды

```bash
# Разработка
npm run dev                  # Dev-сервер на http://localhost:5173

# Сборка
npm run build                # Prod сборка (TypeScript компиляция + Vite)

# Проверка prod-сборки локально
npm run preview              # Запуск статик-сервера из dist/

# Линтинг
npm run lint                 # ESLint + TypeScript проверки всего проекта
npm run lint:eslint          # Только ESLint (с автофиксом)
npm run lint:ts              # Только TypeScript проверка типов
```

## 💡 Реальный опыт использования

Я использую этот конструктор для своего портфолио уже 3 месяца. Вот что реально удобно:

- **Обновление портфолио занимает 2 минуты** - открыл страницу, кликнул на проект, поменял статус с "В разработке" на "Завершён", добавил пару скриншотов. Готово.

- **Можно показать работодателю разные версии резюме** - дублирую основное, убираю пет-проекты, оставляю только коммерческие. Или наоборот - для стартапа показываю все свои эксперименты.

- **Инлайн-редактирование реально экономит время** - не нужно искать форму редактирования, переключаться между страницами. Увидел опечатку - сразу исправил.

- **Firebase бесплатен для таких объёмов** - у меня 20 проектов, 3 версии резюме, куча скриншотов. Все это занимает ~10MB в БД. Firebase Spark план даёт 1GB бесплатно.

**Минусы, которые я заметил:**

- Base64 изображения грузятся медленнее обычных - для 10+ скриншотов заметно. Планирую переехать на Firebase Storage.
- Нет версионирования - если случайно удалил что-то важное, не восстановишь (можно сделать экспорт в JSON через Firebase Console).
- SEO никакой - это SPA без SSR. Для портфолио не критично, но если нужны позиции в Google - придётся добавлять pre-rendering.

## ⚠️ Известные ограничения

- **Аутентификация упрощённая** - для личного использования ок, для публичного сервиса нужна нормальная auth
- **Изображения в base64** - удобно, но не оптимально по размеру и скорости загрузки
- **Нет оффлайн-режима** - все операции требуют интернета (можно добавить PWA + Service Worker)
- **Один уровень отмены** - нет истории изменений и возможности откатиться назад
- **Нет поиска по резюме** - если резюме много, придётся скроллить список

Но для личного портфолио с 1-3 резюме всё это не критично. Проект создан с фокусом на простоту и скорость разработки, а не на enterprise-фичи.

## 🤝 Обратная связь и контрибьюты

Буду рад любому фидбеку! Если нашёл баг, есть идея фичи или просто хочешь поделиться опытом использования - пиши в Issues.

Форки и Pull Requests приветствуются 🚀

## 📝 Лицензия

MIT - делай что хочешь с этим кодом

---

**Сделано с разочарованием в HeadHunter и любовью к гибкости** ❤️
